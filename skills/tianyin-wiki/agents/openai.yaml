interface:
  display_name: "Tianyin Wiki"
  short_description: "生成天印详设并同步到 Confluence"
  default_prompt: "Use $tianyin-wiki. MUST select exactly one route before acting: local generation, document lint, remote read-only, remote publish, or environment repair. Reuse confirmed inputs; read only route-required materials; never scan the workspace or run doctor outside environment repair. Local Markdown is the source of truth. Remote publishing requires explicit user confirmation plus a target page/config; never auto-publish or expose credentials."
