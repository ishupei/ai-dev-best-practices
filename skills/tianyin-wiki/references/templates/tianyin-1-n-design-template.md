# 1-N 详设模版

## 一、需求/项目详情

**产研地址：** 待填写

**需求地址：** 待填写

**发布平台地址：** 待填写

**需求简述：**

待填写。不要直接完整复制产品需求，说明本次改动的目标、范围和关键约束。

## 二、项目分支及影响说明

**天印版本：** 待填写。需求单必须明确版本号；未明确时需补充。

**天印分支：** 待填写，例如 `feature/6.0.14.0`

**插件分支：** 待填写。分支名称应具备可辨识性，避免使用过度简写。

**独立小工具分支：** 不涉及/待填写

**影响说明：**

- 插件：待填写，例如 `plugin-manage`、`plugin-signs`
- 基线后端：待填写，例如 `esign-base-platform`、`esign-doc-sign`

## 三、开发思路

说明代码改造逻辑、涉及读写的数据库字段和关键处理流程；复杂流程可补充 Mermaid 图。

```text
待填写
```

## 四、需求影响范围

### 4.1、历史数据

本次改动是否涉及表结构变更，例如新增字段、修改字段长度或类型、删除字段。

**数据处理：**

- 新增非必填字段：通常无需处理，但需明确说明。
- 新增必填字段：说明历史数据的默认值填充方式，并提供具体 SQL 脚本。
- 状态字段变更：说明是否需要批量更新历史状态，并提供具体 SQL 脚本。

### 4.2、历史关联功能需求

1. 分析本次新增或修改功能与既有功能是否存在关联、冲突或替代关系。
2. 明确是否需要对历史功能做兼容处理、联动改造或下线计划。

## 五、接口详情

**接口名称：** 待填写

**接口地址：** `/path/to/api`

**接口协议：** HTTP

**接口请求方式：** POST/GET

**接口 CONTENT-TYPE：** `application/x-www-form-urlencoded` 或 `application/json`

**请求参数定义：**

| 序号 | 参数名称 | 参数描述 | 参数类型 | 是否必填 |
| --- | --- | --- | --- | --- |
| 1 | userName | 用户姓名 | string | 是 |

**响应参数定义：**

| 序号 | 参数名称 | 参数描述 | 参数类型 | 参数说明 |
| --- | --- | --- | --- | --- |
| 1 | code | 响应码 | int | 0=成功，1=失败 |

**请求参数示例：**

```json
{}
```

**响应参数示例：**

```json
{}
```

## 六、AI提示词

```text
待填写
```

## 七、SQL

1. 表名不包含库名，例如 `esign6_docs.doc_flow` 统一写成 `doc_flow`。
2. 交付后如 SQL 有更新，需同步更新本节。

```sql
-- 待填写
```

## 八、配置

涉及 ecos 或 Nacos 配置时，需写明 `data-id`。

```yaml
# 待填写
```

## 九、其它

可包含问题 QA、客户文档、部署说明等；不涉及时填写“不涉及”。
